source 'https://rubygems.org'
ruby '2.1.2'

gem 'dalli'
gem 'hipchat-api'
gem 'rufus-scheduler', require: false

group :development, :test do
  gem 'dotenv'
end

group :production do
  gem 'memcachier'
end
